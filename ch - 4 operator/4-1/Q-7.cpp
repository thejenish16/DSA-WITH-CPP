#include<iostream>
using namespace std;

main ()
{
    float x , y , z , result;

    cout << "Entar the value of x: ";
    cin >> x;

    cout << "Entar the value of y: ";
    cin >> y;

    cout << "Entar the value of z: ";
    cin >> z;

    result = (x + y + z) * (x + y + z) ;
}